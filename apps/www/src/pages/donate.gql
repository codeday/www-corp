query DonateQuery {
  cms {
    mission: strings(where: { key: "common.mission" }) {
      items {
        value
      }
    }
    tagline: strings(where: { key: "common.tagline" }) {
      items {
        value
      }
    }
  }
}
