// Component style overrides are now handled via recipes and slot recipes in Chakra UI v3.
// The v2 mode()-based component overrides are not compatible with v3's styling system.
const components = {};
export default components;
